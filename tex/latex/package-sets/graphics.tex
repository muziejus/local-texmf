\usepackage[dvipsnames]{xcolor} % For color MUST BE LOADED BEFORE PGFPLOTS
\usepackage{tikz, pgfplots}
\pgfplotsset{compat=1.8}

\usetikzlibrary{
  shapes.multipart,
  arrows.meta, % arrow styling
  chains, % nodes on chains
  positioning,
  quotes, % quoted text on edges? 
  matrix,
  fit % fit nodes around nodes
}


\tikzset{
  n/.style={circle, draw},
  s/.style={arrows={-Stealth[length=3mm]}},
  array/.style={
    rectangle split, rectangle split parts=#1, 
    draw, anchor=center, rectangle split horizontal
  },
}

\usepackage{forest}

\forestset{
  default preamble={%
    for tree={circle, draw, math content}
  },
}

\usepackage{multicol} % for multicolumn layout
\usepackage{tcolorbox} % For colored boxes
